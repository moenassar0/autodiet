import { AdminBase } from "../../layouts/AdminBase"

export const AdminRecipes = () => {
    return(
        <AdminBase navbarProps={<></>} navbarTitle="Recipes" addButton={<></>} dataTable={<></>}>
            
        </AdminBase>
    )
}